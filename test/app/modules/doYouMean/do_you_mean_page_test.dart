import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('DoYouMeanPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(DoYouMeanPage(title: 'DoYouMean')));
    //  final titleFinder = find.text('DoYouMean');
    //  expect(titleFinder, findsOneWidget);
  });
}
